# Goose Agent private maintenance notes

This repository is maintained privately by eachann1024. The macOS app is named **Goose Agent**; keep the `dev.bybee.herdrm` bundle identifiers stable for local data and package compatibility. `HerdrM` remains the Swift target/module name unless a task explicitly requires otherwise.

## Local build

On macOS with Xcode and XcodeGen installed:

```sh
make build
```

Avoid adding release automation, public update feeds, or upstream marketing assets.
